# 日语学习工具

一个自己做的日语学习网页工具，含口语练习和跟读练习两大模块。

## 关键地址

- **网站访问地址（分享用，带邀请码门禁）**: https://japanese-learning.edgeone.dev/
- **网站直接地址（跳过门禁，仅自己用）**: https://japanese-learning.edgeone.dev/日语学习.html
- **本地文件夹**: `/Users/tan/Documents/個人/日语跟读网站`（2026-07-14 从 `50篇演讲` 改名+重组，见下方「邀请码门禁页」）
- **GitHub 仓库**: https://github.com/puux213/japanese-learning
- **部署平台**: 腾讯云 EdgeOne Pages（国内可访问，无需 VPN）
- **Notion 部署文档**: https://app.notion.com/p/39b0009ab78681c285dce62720d78501
- **Notion 任务清单**（下一步任务 / 网站更新想法）: https://app.notion.com/p/39c0009ab78681688c59e2ecebc19bc2

需要看 Notion 内容时告诉 Claude，Claude 再去读取——不设自动 hook 抓取（2026-07-13 试过 SessionStart/Stop 自动同步，后来改成手动按需读取，页面结构也在调整中）。

## 功能模块

- 🗣️ **口语练习**：仿雅思口语，每天一个话题，支持录音、AI 追问、自定义题库
- 🎧 **跟读练习**：50 篇日语 TED 演讲，音频 + 日文原文 + 中文对照 + 生词
- 🎤 **英语跟读**：TED 英语演讲，音频 + 英文原文 + 中文对照 + 生词，结构和跟读练习一致（详见下方「英语跟读模块」）
- 🔍 **中→日翻译工具**：支持多个 AI（DeepSeek/智谱/Groq/Gemini/Claude），带罗马音+假名+语法讲解
- 📓 **生词本**：一键添加，支持导出 CSV
- 🧩 **语法解析**：在任意页面选中一段日语文本右键，AI 自动分析语法点/翻译/用法（见下方「语法解析功能」）
- 📚 **日常用语**：图标网格入口进各主题模块（运动系列、餐厅预约…），点开是折叠卡片列表，装单词/场景对话（详见下方「日常用语模块」）

## 同步流程（怎么把本地改动更新到网站）

三层链路：
**改动步骤**：
1. 在 `/Users/tan/Documents/個人/日语跟读网站` 里改/加文件
2. 双击 `同步到网站.command` 脚本
3. 弹终端窗口 → 看到「✅ 同步完成！」→ 关掉
4. 等 3-5 分钟 EdgeOne 自动重新部署完成
5. 网站更新（所有访问者刷新页面即可看到）

**同步是整仓库一起推送**：`index.html`（带验证码门禁）和 `日语学习.html`（跳过门禁的直接地址）永远一起更新，做不到"只更新有验证码的入口"（2026-07-15 确认，见下方「已知局限」）。

## 本地脚本

| 脚本文件 | 作用 |
| --- | --- |
| `同步到网站.command` | 把本地改动推到 GitHub，触发 EdgeOne 部署 |

首次双击可能弹「无法验证开发者」→ 右键 → 打开 → 授权一次即可。

**终端快捷键**（2026-07-15 新增，配在 `~/.zshrc` 里）：在终端输入 `nihongo` 直接 `cd` 到本项目目录并启动 `claude`，跟 `tomo`（tomography 项目同款快捷键）一个套路。改完 `~/.zshrc` 要 `source ~/.zshrc` 或开新终端窗口才生效。

**日常使用只用网址** `https://japanese-learning.edgeone.dev/日语学习.html`（建议存书签），不要再用本地 http 服务器打开。原来的 `启动日语工具.command`（离线用，起 localhost:8000）已删除——localhost 和网址是浏览器眼中两个不同的网站，localStorage 各存一份互不相通，之前混用导致跟读进度/标记时对时不对（2026-07-14 发现并统一为只用网址）。

## 邀请码门禁页（2026-07-14 新增）

`index.html`（仓库根目录）是访问网站前的一道邀请码验证页，替代原来直接访问 `日语学习.html`：
- 用户打开 `https://japanese-learning.edgeone.dev/` → 看到「请输入邀请码」界面 → 验证通过后用 iframe 加载 `日语学习.html`（同源相对路径，EdgeOne / Cloudflare Pages 两个部署镜像都能用，不写死域名）
- 通过状态存在 `localStorage`（`inviteCodeVerified`），同一浏览器验证一次后不用重复输入
- **邀请码维护**：只改 `config.json` 的 `validCodes` 数组即可（不用碰 `index.html`），改完照常双击同步脚本上线，也可以直接告诉 Claude 加/删谁的码
- **Notion 客户表联动**（2026-07-15 新增，7-15 精简过字段）：Notion「日语学习工具」页面下的「用户管理」数据库（https://app.notion.com/p/39c0009ab78680a9a2abe603b5a9c6cd）字段：客户名字（标题）/联系方式/邀请码/状态（未使用/已使用/已失效）/发放日期/备注。（最初还建过「使用日期」「来源渠道」两列，用户后来删掉了，简化成现在这 6 个字段）。用户在这张表里手动登记客户和邀请码，**不会自动同步**（Notion 没有主动推送到本地文件的机制）。当用户说「**同步邀请码**」时，Claude 要做：① 用 Notion MCP 查这张表里状态≠已失效 且邀请码非空的行 → ② 把这些邀请码写入 `config.json` 的 `validCodes` 数组（覆盖式替换，不是追加）→ ③ 提醒用户双击 `同步到网站.command` 上线。这个联动是"Claude 按需处理"，不是 GitHub Action 之类的自动化基础设施（用户 2026-07-15 明确选择了这个更简单的方案，不想多维护一层自动化）。
- **生成规则**：一人一码（不共用），方便单独吊销；格式随意（好记的昵称+数字，或纯随机字符串都行）。**不要把姓名/联系方式写进 `config.json`**——它是公开文件，见下面的局限说明；码和人的对应关系记在 Notion 客户管理表里（见「下一步任务」）
- 目前 `config.json` 里的码（`LEARN2024A/B/C`、`STUDY001/002`）还是占位测试码，正式发给人之前要换成真实码
- **「是否使用」没法自动记录**：纯静态站没有后端，网站没有任何机制能知道某个码被谁在什么时候验证通过，这个状态只能人工在 Notion 表里手动更新
- **已知局限（不是真正的安全门禁）**：验证逻辑完全在前端 JS 里跑，`config.json` 本身请求一下就能看到明文邀请码列表；而且 `日语学习.html` 这个直接地址本身没有任何保护，知道的人可以跳过门禁页直接访问。这只是挡住"随手转发链接被陌生人刷到"的软性门槛，不是访问控制机制。**真正加固的两个方案**（2026-07-14 讨论过，用户决定暂不做，之后主动提起再捡起来）：① 轻量：`config.json` 存邀请码的哈希值而不是明文，验证时比对哈希（挡住随手打开文件看的人，但挡不住会改 DevTools 里 JS 的人）；② 彻底：邀请码校验挪到 EdgeOne Pages Functions（服务端），码完全不下发给浏览器（真正安全，但要多写一个后端函数）。**已验证无效的方案**（2026-07-15 试过）：把 `日语学习.html` 改名成随机路径想"挡住直接访问"——实测没用，因为 `config.json` 的 `targetUrl` 必须是明文（门禁页 JS 靠它给 iframe 设置 `src`），改名后的路径照样会被浏览器 fetch 到、验证过的人也能在 DevTools 里直接看到，跟不改名一样不设防，所以已改回原名，不用再提这个思路，除非直接上①②中的一个。

**目录结构（2026-07-14 从 `50篇演讲/` 重组为 `日语跟读网站/`）**：仓库根目录现在放 `index.html`（门禁页）、`config.json`（邀请码配置）、`日语学习.html`（正式内容）、`CLAUDE.md`、`同步到网站.command`；原来整个项目文件夹改名装进了子文件夹 `50篇演讲/`，里面是音频/讲稿等资源（`01.xxx`~`50.xxx` 各篇文件夹、`10篇/` 素材、`E01.xxx`~`E10.xxx` 英语音频）。`.git` 仓库根目录也一并挪到了 `日语跟读网站/`。

## 数据存储

**用户数据**（生词本、进度、笔记、API Keys）存在浏览器 localStorage：
- 每次操作自动保存
- 关浏览器/重启电脑都不影响
- **只在同一个网址下有效**（换设备、换浏览器、清理数据、无痕模式都会丢——localStorage 不跨设备同步，目前没有云端账号系统）

**备份建议**：定期在网页右上角点「📤 导出数据」下载 JSON 存网盘。

## AI Key 配置（用户在网页 ⚙️ 设置里填）

按翻译质量+可用性优先级：
1. **DeepSeek**（国内直连，10元送额度）: https://platform.deepseek.com/api_keys
2. **智谱 GLM-4-Flash**（国内直连，完全免费）: https://bigmodel.cn/usercenter/apikeys
3. Groq / Gemini / Claude（海外，需 VPN）

Key 只存本地浏览器，不会外传。

## 讲稿完成情况

- ✅ 已补：1-10 篇（完整日文原文 + 中文对照 + 生词）
- ⏳ 待补：11-50 篇（只有音频和标题）

**补讲稿的方法**：告诉 Claude「补 X-Y 篇讲稿」，Claude 读 PDF 提取内容 → 更新 HTML → 你双击同步脚本。

## 英语跟读模块（2026-07-12 新增）

结构和「跟读练习」完全一样（日期折叠 → 点开有音频 + 逐段原文/翻译 + 生词 + 时间戳跳转），但英语用的是**整段**划分，不是逐句拆分——因为英文缩写、省略号、引号对话按标点强行切分很容易切错，和中文译文对不上（第一次上线时就踩过这个坑，已修复）。

**代码结构**：`日语学习.html` 里 `LISTENING_KINDS` 配置对象让「跟读练习」和「英语跟读」共用同一套渲染引擎（`renderListening`/`loadNextWeek`/`buildListeningEntry` 等函数都带 `kind: 'jp' | 'en'` 参数），没有复制一份代码。数据分别存在 `PRESET_LISTENING`（日语 50 篇）和 `PRESET_LISTENING_EN`（英语，当前 10 篇）两个数组里，浏览器本地存储对应 `state.listening` / `state.listeningEn`。

**已收录 10 篇**（音频文件夹 `E01.xxx` ~ `E10.xxx`，E 前缀避免和日语的 `01`~`50` 文件夹冲突）：
1. 真正厉害的人都戒掉了玻璃心　2. 真正拉开你与周围人的差距　3. 怎样走出负面情绪　4. 瑜伽如何作用于身体和大脑　5. 运动改变大脑　6. 运气对成功有多重要　7. 再艰难也要跳出舒适圈　8. 如何才能幸福　9. 怎样说话人们才爱听　10. 如何在压力下保持冷静

**如果以后还要补充英语讲稿**（流程仅供参考，Claude 每次开新会话需要重新搭这套脚本，没有存在仓库里）：
1. 需要 `pdftotext -bbox`（poppler，`brew install poppler`）按文字的 x/y 坐标位置切分「正文」和「生词侧栏」两栏——不能用普通文本提取或 `-layout`，因为这批 PDF 是双栏排版（正文＋生词侧栏），普通提取容易把生词词条渗进正文中间。每份 PDF 的分栏临界 x 坐标不固定，需要按文档自动检测（取 x>250 之后第一个宽度 ≥5pt 的间隙）。
2. PDF 抽取出来的中日文字符有的是「部首变体」编码（比如「人」被编码成部首符而非正常汉字），需要 Unicode NFKC normalize，个别没有对应关系的部首（齿风骨马门）要手动替换。
3. 音频用 `ffmpeg -ac 1 -b:a 64k -ar 44100` 转成单声道 64kbps 的 audio.m4a（这批是完整长度演讲 10-20 分钟，不是日语那种 1-2 分钟片段，所以体积会大一些，每篇 2-8MB）。
4. 原始 PDF/MP3 素材文件夹（如 `10篇/`）已加入 `.gitignore`，不会被推送上网站，只有转换后的 `E0N.xxx/audio.m4a` 会同步。

## 日常用语模块（2026-07-15 新增，框架先行）

新顶层 tab「📚 日常用语」，导航形式是「图标网格 → 点开某模块 → 折叠卡片列表」，卡片视觉直接复用跟读练习的 `.listening-item`，不重新写一套。

**代码结构**：`日语学习.html` 里
- `TOPIC_CATEGORIES`：顶层图标网格的模块清单（目前 `sports` 运动系列 🏸、`restaurant` 餐厅预约 🍽️），每项 `{id, icon, name_cn, name_jp}`。
- `PRESET_TOPICS_DATA`：按模块 id 分组的内容，每个模块下是 entries 数组（运动系列的 entry = 一项运动，餐厅预约的 entry = 一个场景如「电话预约」），每个 entry 的 `items` 数组是逐条单词/对话，`{jp, romaji, cn}`，对话类可以加 `speaker` 字段（如"客人"/"店员"）自动渲染成小标签，纯词表不用填这个字段。
- 渲染函数：`renderTopicsGrid()`（图标网格）/ `openTopicCategory()` `closeTopicCategory()`（进出某模块）/ `renderTopicEntries()`（该模块下的折叠卡片列表）/ `toggleTopicEntry()`（展开收起）。当前选中的模块存在 `state.topicsCategory`，卡片展开状态存 `state.topicsExpanded`，跟着 localStorage 走，刷新页面/换 tab 都还在原来的位置。
- 每条 item 都能一键「➕ 加入生词本」（复用已有的 `addVocab()`），逻辑和跟读练习页面的生词条目一致。
- entry 的 `items: []`（空数组）会自动显示「📝 内容待补充」占位，不用改渲染逻辑就能先占位后填内容——跟当年 50 篇演讲先占位再补讲稿一个思路。

**目前内容**：运动系列下羽毛球（8 条词汇）已填，网球/游泳/跑步/篮球是空占位；餐厅预约下电话预约（6 句完整对话）已填，到店入座/点餐是空占位。

**以后要加新模块/新内容**：告诉 Claude「日常用语加 XX 模块」或「补 XX 场景的内容」，改 `TOPIC_CATEGORIES`（加模块）或 `PRESET_TOPICS_DATA`（加 entry/填 items）即可，不用碰渲染代码。

### 用 Notion 自助加内容 →「同步日常用语」（2026-07-15 约定，仿「同步邀请码」）

**目的**：让用户不用把每条内容都用文字描述给 Claude，而是自己在 Notion 表里加行，Claude 批量读进来。这是"内容与代码分离"的落地方式——`日语学习.html` 只管渲染（模板），内容维护在 Notion（数据源）。

**Notion 数据库「日常用语内容」**（建在 Notion「日语学习工具」页面下，和「用户管理」表并列，2026-07-15 已建好并填入电话预约 6 句样例）：https://app.notion.com/p/11d67376d1dc4e15b029544d024088f7 （data source id `efa33e17-de14-4e8d-b1b4-bf1acaf764d5`）。字段：
- **模块**（选择）：餐厅预约 / 运动系列…（对应 `TOPIC_CATEGORIES` 的模块，新模块直接加选项）
- **场景**（选择或文本）：电话预约 / 到店入座 / 点餐…（= 一个 entry，渲染成一张折叠卡片）
- **顺序**（数字）：同一场景内条目的排序
- **说话人**（选择）：客人 / 店员 /（留空 = 纯词表，不渲染小标签 → 对应 item 不写 `speaker` 字段）
- **日文**（文本，必填）→ item 的 `jp`
- **中文**（文本，必填）→ item 的 `cn`
- **罗马音**（文本，可留空）→ item 的 `romaji`；**留空时 Claude 在同步时自动生成**，减少用户手工输入
- **上线**（勾选）：只发布勾选的行，方便存草稿

**当用户说「同步日常用语」时，Claude 要做**：
1. 用 Notion MCP 查「日常用语内容」表里 `上线 = true` 的行
2. 按 模块 → 场景 分组，每组内按 `顺序` 排序，组装成 `PRESET_TOPICS_DATA` 的结构（entry.items = `[{jp, romaji, cn, speaker?}]`）
3. 罗马音为空的行，Claude 补上 romaji（纯词表 speaker 留空则 item 不写 `speaker` 字段）
4. 若出现 `TOPIC_CATEGORIES` 里还没有的新模块 → 往 `TOPIC_CATEGORIES` 加一项（`{id, icon, name_cn, name_jp}`，icon 用合适的 emoji，拿不准就问用户要哪个图标）
5. 覆盖式写入 `日语学习.html` 的 `PRESET_TOPICS_DATA`（同一模块整体替换，不是零散追加）
6. 提醒用户双击 `同步到网站.command` 上线

和「同步邀请码」一样，这是"Claude 按需处理"，不是 GitHub Action 之类的自动化基础设施。用户只维护 Notion，Claude 负责把 Notion 翻译成代码。

## 语法解析功能（2026-07-13 新增）

网页右侧固定三张卡片：🔍 翻译 / 📓 生词本 / 🧩 语法（`.side-panel` 用 `position: sticky` 固定在右上角，随内容滚动跟着走，移动端窄屏自动取消 sticky）。

**语法卡片用法**：在页面任意位置（跟读练习的日文段落、翻译结果等）选中一段日语文本后右键，会拦截默认右键菜单，把选中文本（连同能抓到的中文对照，比如所在段落的 `.cn` 译文）发给 AI，返回「语法点 / 翻译 / 怎么使用（含例句）」，渲染在语法卡片里，也能一键加入生词本。触发逻辑在 `日语学习.html` 的 `document.addEventListener('contextmenu', ...)` + `analyzeGrammar()`。

**AI 供应商链路**：新增 `callDeepseek()` / `callZhipu()`（OpenAI 兼容的 chat completions 接口），和已有的 `callGroq()` / `callClaude()` / `callGemini()` 一起被 `callAIChain()` 统一调用，按「DeepSeek → 智谱 → Groq → Claude → Gemini」顺序尝试第一个配置了 Key 的供应商（国内直连优先）。中→日翻译工具（`translateText()`）也改用了这条统一链路，不再各写一遍。Key 输入框在 ⚙️ 设置弹窗里，存在 `state.settings.deepseekKey` / `zhipuKey`。

**顺带修的 bug**：跟读练习（日语）逐句拆分对照时，如果某段日文和中文按句号拆出来的句子数不一致（译者合并/拆分了句子），旧逻辑会把整段中文塞进第一句、后面几句译文变空白，出现"日语和中文翻译没对上"的现象。检测发现 1-10 篇里 `ted_02/03/06/07/08` 都有这个问题。修法和英语跟读模块当初踩的坑一样：句子数对不上时就不再逐句拆，整段保留成一条（`autoSplitParagraphs()`），保证译文永远和原文对应，代价是那几段可点击跳转的粒度变粗。**已加载过跟读内容的用户**需要点一下跟读练习页面里的「🔄 更新讲稿」按钮才会应用到本地已保存的数据（新加载的篇目自动生效）。

## GitHub 认证

已用 Personal Access Token 配置在本地 `.git/config` 里的 remote URL 中。
- ⚠️ 不要把整个文件夹/`.git/config` 发给别人
- 如 Token 泄漏，去 https://github.com/settings/tokens 撤销后重新生成

## 常见改动示例

告诉 Claude：
- 「补 11-15 篇讲稿」→ 读 PDF 更新数据
- 「加一个 XX 功能到网页」→ 改 HTML
- 「口语题库加一批话题」→ 改 PRESET_TOPICS 数组
- 「修改配色/字体」→ 改 CSS
- 「不显示某个按钮了」→ 找到对应位置删除或隐藏

改完后**必须**双击 `同步到网站.command` 才会上线，光在本地看得到不算。

## 依赖信息

- HTML 是单文件应用（一个 .html 装所有 JS/CSS）
- 音频文件是 audio.m4a（每篇 ~2MB，从原 mp4 视频用 ffmpeg 提取）
- Python3 用来跑本地 http 服务器（Mac 自带）
- Git 用来推 GitHub（Mac 自带）
- 无需 npm、无需构建工具

## 🚧 下一步任务（想法阶段，未开工）

**1. 日常用语模块（框架已上线，2026-07-15，内容持续补充中）**
详见上方「日常用语模块」一节。当前只做了两个入口模块（🏸 运动系列、🍽️ 餐厅预约），当初讨论过的购物/坐飞机/研究室场景/工作场景等还没做——框架已经通用化，之后想加哪个直接告诉 Claude「日常用语加 XX 模块」即可，不用重新设计。
- 运动系列：羽毛球已填 8 条词汇，网球/游泳/跑步/篮球还是空占位，要补的话告诉 Claude「运动系列补 XX 的词汇」
- 餐厅预约：电话预约已填 6 句完整对话，到店入座/点餐还是空占位

## 🚧 进行中：ICP 备案 + 自定义域名

**背景**：现在 `japanese-learning.edgeone.dev` 是「Global (MLC excluded)」区域，大陆很多朋友打不开（返回 401 UNAUTHORIZED）。买域名 + ICP 备案是唯一让国内稳定访问的正规路子。

**当前状态**（2026-07-15 据 Notion 更新）：买域名/实名认证/提交 ICP 备案三步已完成，卡在第 4 步「云厂商初审」，等 010 区号的电话核实。备案进度查询地址：https://console.cloud.tencent.com/beian/manage

**完整流程**（约 2-3 周）：

| # | 步骤 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 买域名（腾讯云/阿里云） | ✅ 已完成 | 55 元/年左右 |
| 2 | 域名实名认证 | ✅ 已完成 | 上传身份证 |
| 3 | 提交 ICP 备案 | ✅ 已完成 | 身份证 + 手持照 + 备案幕布 |
| 4 | 云厂商初审 | 🚧 进行中 | 1-2 天，等 010 电话核对 |
| 5 | 管局审核 | ⏳ | 10-15 个工作日 |
| 6 | 收到备案号 | ⏳ | 短信通知「京 ICP 备 xxx 号」 |
| 7 | EdgeOne 绑定域名 | ⏳ | 域名管理 → 添加自定义域名 → 填域名 → 配 CNAME 解析 |
| 8 | 大陆访问打通 | ✅ | 分享给朋友的最终 URL |

**备案完成后的绑定操作**（第 7 步细节）：
1. EdgeOne 控制台 → japanese-learning 项目 → 域名管理
2. 点「添加自定义域名」
3. 填入你的域名（比如 `japanese.你的域名.com`）
4. EdgeOne 会给你一个 CNAME 值
5. 回到腾讯云 DNS 控制台 → 加一条 CNAME 记录（主机记录 `japanese`，记录值 = EdgeOne 给的那串）
6. 等 5-30 分钟解析生效
7. 回 EdgeOne 域名管理页面看到「已生效」→ 分享给朋友

**备案期间可以用的临时方案**：
- 朋友挂 VPN 继续用现在的 `japanese-learning.edgeone.dev` 链接
- 或告诉朋友先等 2-3 周

**Cloudflare Pages 备份部署（备案期间可选）**：
Cloudflare 在国内访问不稳定（GFW 拦），但某些用户/网络能通，作为兜底可以试。
配置方法：dash.cloudflare.com → Workers & Pages → Create application → Pages → Connect to Git → 授权 GitHub 账号 `puux213` → 选仓库 `japanese-learning` → Production branch `main` → Framework preset **None** → Build command **留空** → Build output directory `/` → Save and Deploy。之后每次 `git push` 会同时触发 EdgeOne 和 Cloudflare 两个平台自动部署。给朋友双链接兜底：主用 EdgeOne，打不开试 Cloudflare `.pages.dev`。

**注意事项**：
- 备案是绑定到「主体」（个人身份证）+「域名」的，一次通过之后同一个域名下的子域名（`japanese.xxx.com`、`www.xxx.com`）都能直接用
- 备案期间腾讯云会电话核实，接听时确认「个人办公学习使用，不含商业行为」即可
- 备案号下发后要贴在网站页脚（可以以后再做，不影响使用）

## Notion 任务清单同步约定（2026-07-15）

Notion「日语学习工具」页面（https://app.notion.com/p/39b0009ab78681c285dce62720d78501 ）下有个「任务」区块，目前三项：
1. **改域名（ing）**——即下方「ICP 备案 + 自定义域名」章节，最新进度已同步在本文件里
2. **搭建 notion 网站日常对话内容更新库**——对应下方「日常用语模块」里已经在用的「日常用语内容」Notion 数据库联动（同步日常用语），这项本身框架已经搭好，Notion 那边这条任务条目更像是个入口指针，不是新工作
3. **发新视频**（2026-07-15 发现，之前漏看）——对应下方「小红书涨粉推广」的「账号运营视频更新计划」库，已经开始填内容

**约定**：新开对话时 Claude 应该主动去查一下这个任务清单页面有没有更新，同步进本文件（包括查任务区块下挂的数据库实际内容，不能只看标题/是否为空，2026-07-15 就漏看过一次「发新视频」）。之后用户说「继续」，Claude 不用反问"继续哪个"，而是直接报出当前这份任务清单（改域名 / 日常用语内容库 / 发新视频），问用户要从哪个开始。

## 🚧 进行中：小红书涨粉推广（2026-07-15 Notion 新发现，同日已开始填内容）

**现阶段目标**：迅速吸粉到 1000 人。**操作方式**：宣传网站，关注主包可领取验证码免费试用网站。

**「账号运营视频更新计划」**（Name / Date / Text，https://app.notion.com/p/39c0009ab786804aadcac50d6fb5b801 ）目前 2 条：
- **youtube朋友间应答日语练习**（日期 2026-07-13，文案未填）
- **运动**（无日期，文案：「说『日常用语加购物模块』或『运动系列补网球词汇』这种话就能直接扩展」——像是想拍一个"一句话让 Claude 扩展日常用语模块"的演示视频）

**「资料库」**（https://app.notion.com/p/39c0009ab7868019a633f7d91e018572 ，只有 Name 字段）目前 3 篇旧参考笔记，内容偏旧（提到已删除的 `启动日语工具.command`、旧域名 `japanese-learning-dpzwzu4qwmtv.edgeone.dev`），更像是早期部署笔记，不是视频素材：
- 网站部署、动态更新计划（只是链到账号运营视频更新计划库的空壳页）、网站使用说明

跟进方式待用户决定——两条视频条目还需要具体文案/脚本，「资料库」目前对视频制作没有直接可用素材。

## 更新日志

**2026-07-15**
- 新增「📚 日常用语」顶层模块（框架先行）：图标网格入口 → 点开模块 → 折叠卡片列表，复用跟读练习的卡片视觉。目前上线 🏸 运动系列（羽毛球词汇已填，其余空占位）+ 🍽️ 餐厅预约（电话预约对话已填，其余空占位），详见上方「日常用语模块」。已在浏览器里过了一遍完整流程（进模块/展开卡片/加生词本/刷新页面状态保留），无 console 报错
- Notion「日语学习工具」页面下建了「用户管理」数据库（客户名字/联系方式/邀请码/状态/发放日期/备注），并把 config.json 里当前的 5 个占位测试码登记了进去，详见上方「邀请码门禁页」的「Notion 客户表联动」
- 约定了「同步邀请码」的处理流程：Claude 读 Notion 表里状态≠已失效的邀请码 → 覆盖写入 config.json 的 validCodes → 提醒双击同步脚本上线（按需处理，不是自动化基础设施）
- `~/.zshrc` 加了 `nihongo` 终端快捷键，一键 cd 进本项目 + 启动 claude
- 修了跟读练习（日语 50 篇 + 英语 10 篇）音频播放 404 的 bug：7-14 目录重组后音频移到了 `50篇演讲/` 子文件夹，但 `folder` 字段没跟着改，导致请求路径少了这层前缀。在 `buildListeningEntry`/`refreshPresetContent`（两处组装 `folder` 的地方）统一加了 `LISTENING_ASSET_BASE = '50篇演讲/'` 前缀，用 curl 验证过修复后的路径能拿到正确音频文件。**已加载过跟读内容的用户**需要点一下「🔄 更新讲稿」按钮才会应用到本地已保存的数据（新加载的篇目自动生效）
- 讨论"把 日语学习.html 挪到需要验证码才能访问的路径"想加固门禁，实测发现改名对防护没有实际作用，已改回原名，详见上方「邀请码门禁页」的「已知局限」

**2026-07-14**
- 新增邀请码门禁页 `index.html` + `config.json`（详见上方「邀请码门禁页」），接管了原本直接暴露的 `日语学习.html` 入口
- 项目文件夹改名重组：`50篇演讲/` → `日语跟读网站/`（原文件夹变成里面存音频资源的子文件夹），`.git` 仓库根目录跟着挪到新的顶层目录
- 删除 `启动日语工具.command`（离线用本地服务器脚本）：发现 localhost 和网址是浏览器眼中两个不同网站，localStorage 各存一份，混用导致跟读进度/完成标记看起来"每次打开都没保存"，统一只用网址后问题消失

**2026-07-13**
- 新增「🧩 语法」功能：选中任意日语文本右键 → AI 自动讲解语法点/翻译/用法，详见上方「语法解析功能」
- DeepSeek / 智谱 GLM-4-Flash 真正接入代码（此前只在文档里写了，设置弹窗和调用逻辑其实没做），中→日翻译工具也一起用上了统一的多供应商链路
- 修了跟读练习（日语）1-10 篇里 5 处日中文对照逐句拆分后错位/漏译的 bug（ted_02/03/06/07/08）
- 翻译/生词本/语法三张卡片改成 sticky 固定在右侧，滚动页面时始终跟着到右上角

**2026-07-12（晚上）**
- 上线后发现问题：EdgeOne 免费 `.edgeone.dev` 域名固定为「Global (MLC excluded)」，大陆用户 401 无法访问
- 排查了所有能改的设置：项目设置里没有区域切换选项，域名管理里只有「添加自定义域名」入口
- 确认唯一解决方案：走 ICP 备案 + 绑定自定义域名（约 2-3 周流程）
- 已开始申请域名，等备案下来后回来绑定

**2026-07-12（下午）**
- 新增「🎤 英语跟读」模块，和「跟读练习」共用同一套渲染引擎（详见上方「英语跟读模块」章节）
- 收录英语 TED 演讲 10 篇（完整原文+中文对照+生词），音频转为压缩后的 m4a
- 修了一个上线时就发现的 bug：英语内容最初按逐句拆分导致短句无译文、长句译文错位，改成按段落整段划分

**2026-07-12**
- 部署到腾讯云 EdgeOne Pages（国内可访问）
- 关联 GitHub 自动化部署
- 补充第 1-10 篇 TED 演讲完整讲稿+生词
- 添加国内 AI 支持（DeepSeek + 智谱 GLM）
- 加了"关闭原文/翻译"两个独立开关
- 首页只显示 7 篇，加载下一周按钮
- 空格/左右键快捷键控制音频
- 音频文件从 mp4 提取为 m4a 减小体积

**2026-07-11**
- 初版工具完成（口语 + 跟读双模块）
- 加入 5 篇预设 TED 演讲
- 支持导入/导出数据 JSON
